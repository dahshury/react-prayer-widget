# FSD Pages Layer

This directory contains Feature-Sliced Design (FSD) page slices, **not** Next.js Pages Router routes.

All Next.js routes are defined in the `app/` directory (App Router).

This directory is part of the FSD architecture and should not be treated as Next.js routing.
